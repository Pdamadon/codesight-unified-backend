import { Server as HttpServer } from 'http';
import { DataProcessingPipeline } from './data-processing-pipeline';
export declare class UnifiedWebSocketServer {
    private wss;
    private clients;
    private sessionClients;
    private logger;
    private dataProcessingPipeline;
    private heartbeatInterval?;
    private cleanupInterval?;
    private maxConnections;
    private connectionTimeout;
    constructor(server: HttpServer, dataProcessingPipeline: DataProcessingPipeline);
    private setupWebSocketServer;
    private handleConnection;
    private handleMessage;
    private handleAuthentication;
    private handleSessionStart;
    private handleSessionStop;
    private handleInteractionEvent;
    private handleScreenshotData;
    private handleSessionComplete;
    private handleSubscribeUpdates;
    private handleDisconnection;
    private sendToClient;
    private sendError;
    private broadcastToSession;
    private validateApiKey;
    private getClientFeatures;
    private subscribeToProcessingUpdates;
    private startHeartbeat;
    private startCleanupInterval;
    getStats(): any;
    close(): Promise<void>;
}
