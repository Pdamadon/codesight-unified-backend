{
  "name": "codesight-crowdsource-collector",
  "version": "1.0.0",
  "description": "CodeSight crowdsourced online shopping behavior data collection platform",
  "main": "backend/dist/server.js",
  "scripts": {
    "build": "cd backend && npm ci && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}